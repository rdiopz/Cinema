<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Cinema" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@color/menu_and_header</item>
        <item name = "android:navigationBarColor">@color/menu_and_header</item>
    </style>

    <style name="Theme.Cinema" parent="Base.Theme.Cinema" />

    <style name = "Theme.App.Starting" parent="Theme.SplashScreen">
        <item name = "windowSplashScreenBackground">@color/app_bg_color </item>
        <item name = "windowSplashScreenAnimatedIcon">@drawable/logo_cinema</item>
        <item name="postSplashScreenTheme">@style/Theme.Cinema</item>
     </style>
</resources>